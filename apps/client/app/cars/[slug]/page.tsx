import Link from 'next/link'
import { notFound } from 'next/navigation'
import { ArrowLeft, Gauge, Fuel, Settings2, ShieldCheck } from 'lucide-react'
import { getVehicle } from '../../../lib/vehicles'

const money = (value: number | null) => value == null ? 'POA' : new Intl.NumberFormat('en-ZA', { style: 'currency', currency: 'ZAR', maximumFractionDigits: 0 }).format(value)

export const revalidate = 60

export default async function VehiclePage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
  const car: any = await getVehicle(slug)
  if (!car) notFound()
  return <main className="koc-shell"><section style={{background:'#111',color:'#fff',padding:'28px 0 70px'}}><div className="koc-container"><Link href="/cars" style={{display:'inline-flex',alignItems:'center',gap:8,color:'rgba(255,255,255,.65)',fontSize:11,fontWeight:800,textTransform:'uppercase',letterSpacing:'.12em'}}><ArrowLeft size={14}/> Back to showroom</Link><div style={{display:'grid',gridTemplateColumns:'1.2fr .8fr',gap:36,marginTop:30,alignItems:'end'}}><div style={{aspectRatio:'16/10',background:'#202020',overflow:'hidden'}}>{car.image_url ? <img src={car.image_url} alt={`${car.make} ${car.model}`} style={{width:'100%',height:'100%',objectFit:'cover'}}/> : null}</div><div><div className="koc-kicker" style={{color:'#e33a48'}}>Vehicle details</div><h1 className="koc-display" style={{fontSize:'clamp(40px,5vw,72px)',marginTop:10}}>{car.make} {car.model}</h1>{car.variant && <p style={{color:'rgba(255,255,255,.62)',marginTop:8}}>{car.variant}</p>}<div style={{fontSize:30,fontWeight:900,marginTop:22}}>{money(car.price)}</div></div></div></div></section><section style={{padding:'60px 0'}}><div className="koc-container"><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:12}}>{[['Year',car.year],['Mileage',car.mileage != null ? `${car.mileage.toLocaleString()} km` : '—'],['Transmission',car.transmission],['Fuel',car.fuel_type],['Colour',car.colour],['Body type',car.body_type]].map(([label,value])=><div className="koc-card" key={String(label)} style={{padding:20}}><div style={{fontSize:10,textTransform:'uppercase',letterSpacing:'.12em',fontWeight:900,color:'var(--koc-muted)'}}>{label}</div><div style={{fontWeight:900,marginTop:8}}>{value || '—'}</div></div>)}</div><div style={{display:'grid',gridTemplateColumns:'1.1fr .9fr',gap:50,marginTop:56}}><article><div className="koc-kicker">About this vehicle</div><h2 className="koc-display" style={{fontSize:'clamp(34px,4vw,54px)',marginTop:10}}>Vehicle overview</h2><p style={{color:'var(--koc-muted)',lineHeight:1.8,marginTop:18}}>{car.overview || car.description || 'A quality pre-owned vehicle selected for the King of Cars showroom.'}</p>{Array.isArray(car.features) && car.features.length > 0 && <><h3 style={{fontSize:24,fontWeight:900,marginTop:42}}>Features & equipment</h3><div style={{display:'grid',gridTemplateColumns:'repeat(2,minmax(0,1fr))',gap:10,marginTop:18}}>{car.features.map((feature:string)=><div key={feature} className="koc-card" style={{padding:14,fontSize:13}}>{feature}</div>)}</div></>}</article><aside className="koc-card" style={{padding:28,alignSelf:'start'}}><ShieldCheck color="var(--koc-red)"/><h3 style={{fontSize:22,fontWeight:900,marginTop:16}}>Vehicle confidence</h3><p style={{color:'var(--koc-muted)',lineHeight:1.7,marginTop:10}}>Vehicle information is maintained from the dealership inventory source. Confirm final specification, availability and pricing with King of Cars before purchase.</p><Link href={`/contact?vehicle=${car.id}`} className="koc-button koc-button-primary" style={{marginTop:22,width:'100%',justifyContent:'center'}}>Enquire about this car</Link></aside></div></div></section></main>
}
