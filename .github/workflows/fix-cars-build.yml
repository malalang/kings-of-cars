name: Fix Cars Server Component CSS

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Replace styled-jsx with plain style
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('apps/client/app/cars/page.tsx')
          text = path.read_text(encoding='utf-8')
          old = '<style jsx global>'
          new = '<style>'
          if old not in text:
              print("No styled-jsx tag found; nothing to change.")
              raise SystemExit(0)
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
          print("Replaced styled-jsx with plain style tag.")
          PY

      - name: Commit fix
        shell: bash
        run: |
          if git diff --quiet -- apps/client/app/cars/page.tsx; then
            echo "No change required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/client/app/cars/page.tsx
          git commit -m "fix(cars): remove styled-jsx from server page"
          git push
